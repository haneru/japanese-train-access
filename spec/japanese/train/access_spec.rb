require "spec_helper"

describe Japanese::Train::Access do
  it "has a version number" do
    expect(Japanese::Train::Access::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
